# Config parameter that controls the ratio of whitebox vs blackbox fuzzing
# 0 = completely greybox
# 0.3 = first 30% greybox, last 70% blackbox
# 1 = complete blackbox
# TODO: set variable
AFL_HYBRID_RATIO=0

# Number of worker threads (default: all cores)
# TODO: set variable
WORKERS=8

# Number of workers to allocate for a campaign (default: 1)
# TODO: set variable
CAMPAIGN_WORKERS=8

# Time to run each campaign. (s: seconds, m: minutes, h: hours, d: days).
TIMEOUT=40h

# Time (in seconds) between polls
POLL=30

# Path to directory where shared volumes will be created
WORKDIR=./workdir

# Number of campaigns to run per program (per fuzzer)
REPEAT=1

# Campaigns to run
FUZZERS=(aflplusplus)
aflplusplus_TARGETS=(libpng libtiff lua openssl sqlite3)
aflplusplus_openssl_PROGRAMS=(server bignum x509)

# Ignore
# [WORKER_POOL]: a space-separated list of logical cores to allocate
# WORKER_POOL="1"
