// Save as test_merges.c
#include <stdio.h>

int main() {
    int x = 0;
    
    if (x > 0) x++;
    if (x > 1) x++;
    if (x > 2) x++;
    if (x > 3) x++;
    if (x > 4) x++;
    if (x > 5) x++;
    
    printf("%d\n", x);
    return 0;
}
